<h1>Add User</h1>
<%= form_with(model: @user, local: true, class: "form") do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, required: true, autofocus: true, placeholder: "Name" %>
  </div>

  <%= f.submit nil, class: "green button" %>
<% end %>